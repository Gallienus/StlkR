html
    head
        link(href='/style.css', rel='stylesheet')
        script(src='https://code.jquery.com/jquery-3.1.1.min.js')
        script(src='/tools.js')
        block extrahead
        title Insta Hub
    body
        if navbar
            nav
                each item in navbar
                    span
                        a(href=item.href)= item.description
                span.right
                    a(href='javascript:void(0)')#selectLogin [&ltselect login&gt]
            hr
        block content
        script(src='/activelogin.js')
